- @products.each do |product|
  = product.name
  = product.price
  = product.product_images do |image|
    = image.image
.Top
  = render "header"
  = render "main"
  = render "bottomview_footer"
  = render "exhibition"
